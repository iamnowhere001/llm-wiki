# AGENTS.md — LLM Wiki 维护手册（Schema 层）

> **必读总纲。** 任何 LLM Agent 进入本仓库，先读本文件，再动手。
> 细则**按需读**：规则在 [[schema]]，人类裁定史在 [[decisions]]，人类偏好在 [[conventions]]。
>
> **本文件必须薄。** 它每次会话都进上下文 —— 写在里面的每一行，都在每一次任务里被付费（判据见 [[agents-md]] 的「常驻成本」）。
> 因此这里只放**任何任务都要遵守的东西**；只在特定动作时才需要的细则，一律外置。

---

## 0. 一句话

**不要让 LLM 每次查询都从零检索原始文档。让它增量编译并持续维护一份互相链接的 Markdown 知识库。**

RAG 是「每次现算」，LLM Wiki 是「编译一次、持续保鲜」。知识库是一个**会复利的持久产物**。

---

## 1. 三层架构

| 层 | 位置 | 谁写 | 谁读 | 内容 |
|---|---|---|---|---|
| 原始素材 | `raw/` | 人类 | LLM（只读） | 文章、论文、笔记、PDF、图片 |
| 知识库 | `wiki/` | LLM | 人类 | 摘要、实体、概念、分析、交叉引用 |
| 规范 | `AGENTS.md` + `wiki/schema.md` + `wiki/decisions.md` + `wiki/conventions.md` | 共同演进 | LLM | 结构、约定、工作流、裁定史 |

### 铁律：`raw/` 不可变

LLM 永远不修改、不重命名、不删除 `raw/` 下的任何文件。它是事实来源（source of truth）。

需要修正时，**在 `wiki/` 里写清楚**：「素材原文如此，但根据 X 应理解为 Y」。

> [!warning] 唯一的例外是**清理历史遗留的抓取元数据**，须人类裁定，且有 8 条准入条件。
> 见 [[schema]] §3.2。**它不是常态化的可写权限。**

### 三层之外的第 0 问：为什么

三层回答「东西放在哪」，没回答「**为什么要放**」。没有后者，三层会退化成一台自动摘要机 —— 收得越多，熵越大，而没人知道为了什么。

所以 `wiki/projects/` **不是第四层，是三层共同的入口**：目标 → 误差信号 → 决定收什么素材、建什么页。
判据来自 [[cybernetic-learning]]：**没有目标，大脑不会标记什么重要。**

---

## 2. 目录结构

```
llm-wiki/
├── AGENTS.md              # 本文件：必读总纲
├── README.md              # 给人看的快速开始
├── raw/                   # 原始素材（人类写入，LLM 只读）
│   ├── assets/            #   图片、附件
│   └── YYYY-MM-DD-slug.md #   素材文件
├── wiki/                  # LLM 生成并维护的知识库
│   ├── index.md           #   内容索引（每次 ingest 后必须更新）
│   ├── log.md             #   操作日志（append-only）
│   ├── overview.md        #   总览页：这个知识库在讲什么
│   ├── schema.md          #   维护细则：页面规范 + 工作流（按需读）
│   ├── decisions.md       #   裁定档案：为什么这么定（按需读）
│   ├── conventions.md     #   人类的使用偏好（回答风格、格式要求等）
│   ├── projects/          #   项目页：入口层 —— 我在做什么、因此什么重要
│   ├── sources/           #   素材摘要页：一份素材一页
│   ├── entities/          #   实体页：人物、组织、工具、产品
│   ├── concepts/          #   概念页：理论、方法、模式、术语
│   └── analyses/          #   分析页：对比、综述、回答归档
├── tools/
│   └── wiki.py            #   零依赖 CLI 工具链
└── templates/             #   页面模板
```

---

## 3. 每次动手必须遵守的硬约束

**这一节是「任何任务都要遵守」的部分，不可省略。** 违反其中任何一条，产物即失效。

1. **`raw/` 不可变** —— 不修改、不重命名、不删除。需要修正时在 `wiki/` 里写「原文如此，但应理解为……」。
2. **素材冲突必须显式标注矛盾**，不允许静默覆盖旧结论。
3. **没有来源的断言必须标 `（未验证）`**，并把相关页 `confidence` 降级。
4. 说「**库里没有 X**」之前，必须先在库内检索，并写明**检索了哪几段**。这是个强断言，说出口前要有检索记录。
5. 引用行区间前**回原文核对一次**。全库默认**文件绝对行号**，页内不再声明坐标系（[[schema]] §3.3）。凭记忆沿用行号是本库已犯过三次的错。
6. **一份素材通常触及 10-15 个页面。只写摘要页是失败的做法。**
7. **收录前先读 `wiki/projects/` 的缺口表** —— 填不上任何缺口的素材，**现在还不该收**。
   缺口表有「文献缺口」与「未知缺口」两类（见 [[schema]] §1.6）。**「未知缺口」不构成「填得上」** —— 声称在填它时，必须说明这份素材**如何把那个洞变得可见**（例如它提供了本库从未有过的材料类型）。
8. **只链接已存在的页面**；每页至少 1 条出链、1 条入链。
9. **`sources` 字段必须指向真正包含该内容的素材**，不允许悬空引用。
10. **不发明项目。** 项目由**人类的意图**派生，不由素材派生。
11. **综合页（`analyses/`）必须逐节点标注证据性质** —— 节点之间是「事实」还是「本库的推断」要分开说。
12. **每次改动后**：`index` → `lint`。**`build` 只在里程碑时**（判据见 [[schema]] §2.1）—— 都不命中就**明确地不构建**。

---

## 4. 指路表

| 我要做什么 | 读哪里 |
|---|---|
| **开始一次会话** | 本文件 → `wiki/index.md` → [[conventions]] |
| 收录一份素材 | [[schema]] §2 收录 |
| 抓取网页 / 判断证据问题 | [[schema]] §3 |
| 建 / 改一个页面 | [[schema]] §1 页面规范 |
| 查询并归档答案 | [[schema]] §4 |
| 体检 | [[schema]] §5 |
| 判断要不要重建站点 | [[schema]] §2.1 构建时机 |
| 追问「这条规则为什么这么定」 | [[decisions]] |
| 人类希望我怎么回答 | [[conventions]] |

---

## 5. 与人类的分工

| 人类负责 | LLM 负责 |
|---|---|
| **提出并定义项目**（含可验收的 `goal`） | 维护项目页的缺口表、知识链接、决策记录 |
| 挑选素材、决定研究方向 | 阅读、分层、核查、拆页、建链、回填 |
| 提出好问题、判断什么重要 | 交叉引用、维护一致性 |
| 最终判断与取舍 | 标注矛盾、跑 lint、写日志 |
| 修改 [[conventions]] 里的偏好 | 把偏好落实到每一次输出 |

人类不写 wiki。**有一件事 LLM 不能代劳**：

1. **不发明项目。** 项目由**人类的意图**派生，不由素材派生。LLM 可以指出「本库没有项目」，但不能替人类决定做什么。

> **两件已废止的事，墓碑见 [[decisions]]：**
> ① 原「第 −1 步：人类先问（HFQ）」于 2026-09-18 移除 —— 现在 ingest 没有任何人类前置条件。
> ② 原「LLM 不代写 HFQ」随之废止。
>
> 剩下的这件（不发明项目）指向的仍然是一句话：**目标只能由人提出，LLM 负责把答案找到并整理好。**
> 本库此前缺的从来不是「答案的整理」，而是**问题是谁提的** —— 这句话在 HFQ 移除后仍然成立，
> 只是它现在**不再由一条硬约束来保证**，而要靠项目页的 `goal` 与人类主动提问来维持。
