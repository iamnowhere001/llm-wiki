# AGENTS.md — LLM Wiki 维护手册（Schema 层）

> 这是三层架构中的**规范层**。它不描述某个具体知识，而是描述「这个知识库如何被维护」。
> 任何 LLM Agent 进入本仓库后，先读本文件，再动手。
> 本文件由人类与 LLM 共同演进 —— 发现更好的做法，直接改这里。

---

## 0. 一句话

**不要让 LLM 每次查询都从零检索原始文档。让它增量编译并持续维护一份互相链接的 Markdown 知识库。**

RAG 是「每次现算」，LLM Wiki 是「编译一次、持续保鲜」。知识库是一个**会复利的持久产物**。

---

## 1. 三层架构

| 层 | 位置 | 谁写 | 谁读 | 内容 |
|---|---|---|---|---|
| 原始素材 | `raw/` | 人类 | LLM（只读） | 文章、论文、笔记、PDF、图片 |
| 知识库 | `wiki/` | LLM | 人类 | 摘要、实体、概念、分析、交叉引用 |
| 规范 | `AGENTS.md` + `wiki/conventions.md` | 共同演进 | LLM | 结构、约定、工作流 |

**铁律：`raw/` 是不可变的。** LLM 永远不修改、不重命名、不删除 `raw/` 下的任何文件。它是事实来源（source of truth）。需要修正时，在 `wiki/` 里写清楚「素材原文如此，但根据 X 应理解为 Y」。

### 三层之外的第 0 问：为什么

三层架构回答的是**「东西放在哪」**。它没有回答一个更靠前的问题：**「为什么要放」**。

没有这个问题，三层会退化成一台自动摘要机 —— 收得越多，熵越大，而没人知道为了什么。
所以 `wiki/projects/` 不是第四层，它是**三层共同的入口**：它给出目标，目标产生误差信号，误差信号决定什么素材值得进 `raw/`、什么页面值得建。

判据来自 [[cybernetic-learning]]：**没有目标，大脑不会标记什么重要。** 这条判据对人和对知识库同样成立。

---

## 2. 目录结构

```
llm-wiki/
├── AGENTS.md              # 本文件：Schema 层
├── README.md              # 给人看的快速开始
├── raw/                   # 原始素材（人类写入，LLM 只读）
│   ├── assets/            #   图片、附件
│   └── YYYY-MM-DD-slug.md #   素材文件
├── wiki/                  # LLM 生成并维护的知识库
│   ├── index.md           #   内容索引（每次 ingest 后必须更新）
│   ├── log.md             #   操作日志（append-only）
│   ├── overview.md        #   总览页：这个知识库在讲什么
│   ├── conventions.md     #   人类的使用偏好（回答风格、格式要求等）
│   ├── projects/          #   项目页：入口层 —— 我在做什么、因此什么重要
│   ├── sources/           #   素材摘要页：一份素材一页
│   ├── entities/          #   实体页：人物、组织、工具、产品
│   ├── concepts/          #   概念页：理论、方法、模式、术语
│   └── analyses/          #   分析页：对比、综述、回答归档
├── tools/
│   └── wiki.py            #   零依赖 CLI 工具链
└── templates/             #   页面模板
```

---

## 3. 页面规范

### 3.1 Frontmatter（每页必需）

```yaml
---
title: 页面标题
type: concept            # source | entity | concept | analysis | meta
slug: llm-wiki-pattern   # 全库唯一，与文件名一致（不含 .md）
tags: [知识管理, LLM, 模式]
created: 2026-09-18
updated: 2026-09-18
sources: [2026-09-18-karpathy-llm-wiki]   # 支撑本页的 raw 素材 slug
related: [rag-vs-wiki, ingest-query-lint] # 相关页面 slug
confidence: high         # high | medium | low —— 证据充分度
status: active           # active | draft | stale | deprecated
---
```

**`slug` 必须与文件名一致**，且全库唯一。工具链靠它解析链接。

### 3.2 页面类型

| type | 目录 | 一页是什么 | 何时创建 |
|---|---|---|---|
| `project` | `projects/` | **我在做的一件事**：目标、当前缺口、消耗的知识、产出 | 人类提出一个要推进的目标 |
| `source` | `sources/` | 一份素材的结构化摘要 | 每次 ingest |
| `entity` | `entities/` | 一个具体的人 / 组织 / 工具 / 产品 | 素材中反复出现的具名对象 |
| `concept` | `concepts/` | 一个抽象的理论 / 方法 / 模式 / 术语 | 素材中反复出现的抽象概念 |
| `analysis` | `analyses/` | 一次对比 / 综述 / 回答归档 | 有价值的问题答案、跨页综合 |
| `meta` | `wiki/` 根 | index / log / overview / conventions | 系统文件 |

**判定规则**：能被指着一张照片说「这是它」的 → entity；只能被描述、不能被拍照的 → concept。
**创建阈值**：一个名字在 ≥2 份素材中出现，或者在一份素材里是核心论点 → 独立成页。只出现一次的次要名词，先在正文里提一句，等它再次出现再建页。

**项目页是唯一的例外 —— 它不由素材派生，而由人类的意图派生。** 所以：
- 它**不需要 `sources`**（lint 已豁免），因为它的来源是「我要做什么」，不是某份素材。
- 它**只能由人类提出**。LLM 不替人类发明项目（见第 8 节分工表）。
- 它是**入口层**：其他页面的价值由「是否服务于某个项目」来检验。

### 3.3 正文结构

每个页面遵循「**先结论，后细节**」：

```markdown
# 标题

> 一句话定义（TL;DR）。读者只看这一行也能知道这页在讲什么。

## 要点
- 3-6 条要点

## 正文
展开论述。用 [[wikilink]] 建立交叉引用。

## 与其他页面的关系
- 区别于 [[x]]：...
- 是 [[y]] 的前置：...

## 待办 / 开放问题
- [ ] 需要补充的资料

## 来源
- [[2026-09-18-karpathy-llm-wiki]]
```

### 3.4 链接语法

- 页内链接：`[[slug]]` 或 `[[slug|显示文本]]`
- **只链接已存在的页面。** 如果 `[[x]]` 指向不存在的页，lint 会报 broken link —— 要么建页，要么去掉链接。
- 每页至少 **1 条出链**（除 `sources/` 外）和 **1 条入链**。孤立页会被 lint 标出。

### 3.5 项目页（入口层）

项目页回答的不是「这是什么」，而是**「我在做什么、因此什么重要」**。它是本库的过滤器生成器 —— 依据来自 [[cybernetic-learning]] 的链条：

```
目标 → 误差信号 → 过滤器 → 相关性 → 留存
```

没有目标，就没有误差信号；没有误差信号，大脑不会标记什么重要。**项目页就是把「目标」和「误差信号」显式写下来的地方。**

#### frontmatter

```yaml
---
title: 项目名
type: project
slug: project-slug
tags: []
created: 2026-09-18
updated: 2026-09-18
goal: 一句话目标，要能被判断「完成没有」   # 项目页特有，必填
stage: planning                          # 项目页特有
started: 2026-09-18
sources: []      # 项目通常留空 —— 它的来源是意图，不是素材
related: []
confidence: high
status: active
---
```

**`stage` 与 `status` 是两个维度，不要混用：**

| 字段 | 回答的问题 | 取值 |
|---|---|---|
| `status` | 这**页**还新鲜吗 | `active` / `draft` / `stale` / `deprecated` |
| `stage` | 这**事**做到哪了 | `planning` / `active` / `paused` / `shipped` / `abandoned` |

#### goal 必须可验收

反例：「学习 Rust」「研究知识管理」。
正例：「用 Rust 写一个能跑的命令行工具并发布到 GitHub」「把本库的历史线补到 1990 年代之前」。

判据：**看到 goal 能回答「做完了没有」**。不能验收的目标产生不了误差信号，等于没写。

#### 正文结构与作用

| 小节 | 作用 | 对应控制论要素 |
|---|---|---|
| 目标（可验收） | 参考信号 | 目标 |
| **当前缺口** | **误差信号** —— 全页最重要的一节 | 误差信号 |
| 知识 | 本项目消耗哪些页面 | 相关性 |
| 产出 | 外向回路 —— 知识流出到哪里 | 留存 / 复用 |
| 决策记录 | 项目里做过的判断（AI 拿不到的部分） | — |
| 不做什么 | 反范围，避免蔓延 | — |

**「当前缺口」是项目页存在的核心理由。** 它必须写成表格：`缺口 / 卡在哪 / 需要什么素材`。这张表直接决定下一步找什么素材。

表格保持三列。需要标优先级时，在「缺口」列内用 `**【阻塞】**` 前缀 —— 它表示这一条直接挡住 `goal` 的验收条件，必须先解；未标的是可选或可延后的。

#### 项目是 ingest 的入口

**这是项目层最重要的作用。** 收录素材前先读 `projects/`，问：

1. 这份素材填的是哪个项目的哪个缺口？
2. 填不上任何缺口 —— 那它**现在还不该收**。

这条规则把「这个内容有意思」换成「这个内容有用」，是 [[cybernetic-learning]] 的过滤器在流程上的落地。**它也是本库对抗「收藏即学习」错觉的主要机制。**

#### 生命周期

- 项目 `shipped` 后**不删除**。它的「决策记录」是后续项目最值钱的参照。
- 项目 `abandoned` 时，在「决策记录」里写清**为什么放弃** —— 这比成功记录更稀缺。
- 项目长期 `paused` 且无产出，考虑降级为 `analyses/` 里的一页，或标 `stage: abandoned`。

---

## 4. 工作流

### 4.1 Ingest（收录素材）

人类把素材丢进 `raw/`，然后说「收录」。

**第 0 步（先于一切）：读 `wiki/projects/`。** 判断这份素材服务于哪个项目的哪个缺口。
- 能对上 → 在 `sources/` 页里写明「服务于 [[项目slug]] 的哪个缺口」，并在项目页的「知识」一节加回链。
- 对不上任何缺口 → **先向人类指出**：「这份素材填不上任何当前缺口，确定要收吗？」

这不是形式主义。它是 [[cybernetic-learning]] 的过滤器在流程上的落地 —— 见第 3.5 节。
**注意：若本库还没有任何项目页，先提醒人类建一个**（lint 会在知识页 ≥10 时提示）。

0. **抓取质量优先于抓取速度。** 网页正文抓取会丢行内链接、把 `→` 转成 `->`、丢表格分隔符。只要目标提供结构化端点，就用它：
   - GitHub 仓库 → `api.github.com/repos/<owner>/<repo>`（元数据）+ `/readme`（base64 正文）
   - Gist → `gist.githubusercontent.com/<user>/<id>/raw`（正文原文）
   - X / Twitter → `cdn.syndication.twimg.com/tweet-result?id=<id>&token=a`（元数据 + 正文预览）
   - 静态文档站 → 直接取 `.md` 源文件而非渲染后的 HTML

   落盘后**校验一次**：正文里应该有链接的地方是否还有链接。捕获缺陷在收录时发现成本最低。

   **若只能用 AI 中介的抓取工具（如渲染页提取），必须交叉验证。** 用独立元数据源比对（例如 syndication 端点返回的 `preview_text` 与正文开头是否逐字一致），并在素材 frontmatter 的 `capture_quality` / `capture_note` 中如实记录可信度与已知缺失（如图片未能下载）。

   **素材含推广内容时必须在 frontmatter 与摘要页标注利益披露**（作者是产品运营者、带 UTM 链接等），并把相关页面的 `confidence` 降级。

   **素材本身是 AI 生成内容时，按以下约定处理**（2026-09-18 由人类裁定，`kind: ai-conversation` 等）：

   | 步骤 | 要求 |
   |---|---|
   | 收录 | **收**。理由是它作为「对照样本」有价值 —— 它示范了「结构完整度 ≠ 证据强度」 |
   | 降级 | 摘要页与由其派生的实体 / 概念页**整页 `confidence: low`** |
   | 引用 | **可以被其他页面引用，但引用时必须标注其 AI 生成性质与 `confidence: low`** —— 不允许像引用一手材料那样直接引用 |
   | 说明 | frontmatter 写明 `kind` 与收录理由；摘要页写明「低置信度的原因」与「仍然收录的原因」 |

   **为什么不禁止引用**：完全禁止会让这类素材无法参与库内论证，而降级 + 标注保留了它作为反例的功能。**注意区别于「推广内容」** —— 推广内容的问题是可追溯性（能追溯但动机有偏），AI 生成内容的问题是可追溯性为零，两者降级理由不同。

1. **读**：完整读一遍素材，含 frontmatter。如果素材引用图片，先读文本，再单独查看关键图片。
2. **对齐**：向人类汇报 3-5 条关键要点，以及它与现有 wiki 的关系（新增？印证？**矛盾**？）。若素材与已有页面冲突，明确指出，不要悄悄覆盖。
3. **写摘要页**：`wiki/sources/<slug>.md`，按模板。slug 取 `YYYY-MM-DD-短标题`。
4. **拆实体 / 概念页**：按 3.2 的阈值，新建或更新 `entities/`、`concepts/`。
5. **回填**：更新所有被影响的已有页面 —— 补充事实、修订结论、标注矛盾。**一份素材通常触及 10-15 个页面，只写摘要页是失败的做法。**
6. **更新索引**：在 `wiki/index.md` 对应分类下加入新页与一行摘要。
7. **写日志**：`python3 tools/wiki.py log ingest "素材标题"`。
8. **自查**：`python3 tools/wiki.py lint`，确保无 broken link。

### 4.2 Query（查询）

1. 先读 `wiki/index.md` 定位候选页（中等规模下比向量检索更可靠）。
2. 规模变大后用 `python3 tools/wiki.py search "<关键词>"` 做 BM25 检索。
3. 读相关页，**带引用地**综合作答。引用格式：`（见 [[page-slug]]）`。
4. **好答案必须归档。** 如果回答包含对比、综合、新连接 —— 写入 `wiki/analyses/<slug>.md`，并回填索引与日志。
   这样探索也会像素材一样复利。
5. 若发现知识缺口，在回答末尾列出「建议补充的素材 / 建议深挖的问题」。

### 4.3 Lint（体检）

人类说「检查一下知识库」时执行。跑 `python3 tools/wiki.py lint` 拿机器可查的部分，再人工/LLM 补下面这些语义问题：

- **矛盾**：两页对同一事实给出不同说法 → 用 `> [!warning] 矛盾` 块标出，并列双方证据。
- **过期**：新素材已推翻旧结论 → 旧页标 `status: stale`，并链接到新页。
- **孤岛**：无入链的页面。
- **该建未建**：被反复提及但没有独立页的概念。
- **数据缺口**：可以靠一次网络搜索补上的空白。
- **建议**：下一批值得找的素材、值得问的问题。
- **项目层**（机器可查的部分工具链已覆盖，语义部分靠人）：
  - 项目页缺 `goal`、`stage` 取值非法、未链接任何知识页 → 工具链直接报错
  - **未被任何项目引用的知识页** → 工具链给提示。逐个人工判断：该挂到某个项目下，还是该考虑不再维护？
  - **长期无产出的项目** → 人工核查。项目页「产出」一节为空且 `stage: active` 超过一段时间，要么降级，要么承认它是收藏而非项目。
  - **缺口过期的项目** → 人工核查。「当前缺口」表里的条目是否还成立？已解决的有没有划掉？

每次 lint 完写一条日志：`python3 tools/wiki.py log lint "本轮发现 N 个问题"`。

---

## 5. index.md 与 log.md

**index.md —— 面向内容。** 全库目录，按分类组织，每条 = 链接 + 一行摘要。每次 ingest 后更新。查询时先读它。

**log.md —— 面向时间。** 只追加，不改写。条目格式固定，便于 grep：

```
## [2026-09-18] ingest | LLM Wiki (Karpathy Gist)
```

保持这个前缀，就可以：`grep "^## \[" wiki/log.md | tail -5`

日志类型：`ingest` / `query` / `lint` / `edit` / `init`。

---

## 6. 工具链

```bash
python3 tools/wiki.py <command>

  init [path]              在当前/指定目录初始化一个新的知识库
  lint                     健康检查：断链、孤岛、缺 frontmatter、重复 slug、项目层
  stats                    统计：各类型页面数、链接数、孤立页、项目阶段分布
  search "<query>"         BM25 全文检索（支持中文二字组）
  index                    从各页 frontmatter 重建 wiki/index.md
  build                    生成单文件浏览站点 site/index.html
  log <type> "<message>"   追加一条日志
  new <type> <slug>        按模板新建页面（type: project / source / entity / concept / analysis）
  graph                    打印链接关系（便于发现枢纽页与孤岛）
```

**零依赖**：只用 Python 标准库，不需要 pip install。

---

## 7. 命名与写作约定

- 文件名：全小写，单词用 `-` 连接，英文 slug（`rag-vs-wiki`）。标题用中文写在 `title` 字段里。
- 素材文件名：`YYYY-MM-DD-slug.md`，日期是**收录日**还是**发布日**取其一并保持一致 —— 本项目用**收录日**。
- **重新抓取同一素材**时，新文件加 `-r2` / `-r3` 后缀（`raw/2026-09-18-slug-r2.md`），**不修改旧文件**。新文件的 frontmatter 用 `supersedes` 指向旧版，用 `capture_note` 说明差异。差异必须同时记入对应的 `sources/` 摘要页。
- 语气：直接、信息密度高。不要「值得注意的是」「综上所述」这类填充词。
- 长度：概念页 60-200 行。超过 300 行说明该拆页。
- 数字与事实必须能追溯到 `sources`。**没有来源的断言要显式标注**：`（未验证）`。
- **`sources` 字段必须指向真正包含该内容的素材。** 如果一页的结论来自某处，而那里没有被收录进 `raw/`，就必须先补录素材，或把 `sources` 留空并显式标注缺口 —— 不允许指向一份并不包含该内容的素材（悬空引用）。
- **二手来源与一手来源冲突时，以一手为准，并记录纠正过程。** 检索到的二手引用常有年份/数字误差（如把 1998 年的演讲记成 1999 年）。拿到原文后若发现不一致，**不要静默采用**，在对应 `sources/` 页与实体页写明「二手来源如此，原文为彼」。这类纠正是知识库的价值之一，不是瑕疵。
- **综合页（`analyses/`）必须逐节点标注证据性质。** 当一页把多份素材串成一条叙述时，**节点之间是「事实」还是「本库的推断」要分开说**。做法：列出每份素材覆盖的区间与其性质（一手/二手/通俗），并单独说明哪些结论属综合判断。默认 `confidence: medium`，除非每个节点都有独立一手来源。
- **单来源的实体页要显式声明。** 若一个实体页的全部内容只来自一份素材（尤其是二手或通俗文献），在页尾用 `> [!note]` 说明，并降 `confidence`。数字类断言（数量、年份、规模）若无一级来源，标注「（未验证）」。

---

## 8. 与人类的分工

| 人类负责 | LLM 负责 |
|---|---|
| **提出并定义项目**（含可验收的 `goal`） | 维护项目页的缺口表、知识链接、决策记录 |
| 挑选素材、决定研究方向 | 阅读、摘要、拆页 |
| 提出好问题、判断什么重要 | 交叉引用、回填、维护一致性 |
| 最终判断与取舍 | 标注矛盾、跑 lint、写日志 |
| 修改 `conventions.md` 里的偏好 | 把偏好落实到每一次输出 |

人类不写 wiki。**LLM 不发明项目** —— 这是唯一一条 LLM 不能代劳的选题。LLM 可以指出「本库没有项目」，但不能替人类决定做什么。
