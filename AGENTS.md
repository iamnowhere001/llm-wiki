# AGENTS.md — LLM Wiki 维护手册（Schema 层）

> 这是三层架构中的**规范层**。它不描述某个具体知识，而是描述「这个知识库如何被维护」。
> 任何 LLM Agent 进入本仓库后，先读本文件，再动手。
> 本文件由人类与 LLM 共同演进 —— 发现更好的做法，直接改这里。

---

## 0. 一句话

**不要让 LLM 每次查询都从零检索原始文档。让它增量编译并持续维护一份互相链接的 Markdown 知识库。**

RAG 是「每次现算」，LLM Wiki 是「编译一次、持续保鲜」。知识库是一个**会复利的持久产物**。

---

## 1. 三层架构

| 层 | 位置 | 谁写 | 谁读 | 内容 |
|---|---|---|---|---|
| 原始素材 | `raw/` | 人类 | LLM（只读） | 文章、论文、笔记、PDF、图片 |
| 知识库 | `wiki/` | LLM | 人类 | 摘要、实体、概念、分析、交叉引用 |
| 规范 | `AGENTS.md` + `wiki/conventions.md` | 共同演进 | LLM | 结构、约定、工作流 |

**铁律：`raw/` 是不可变的。** LLM 永远不修改、不重命名、不删除 `raw/` 下的任何文件。它是事实来源（source of truth）。需要修正时，在 `wiki/` 里写清楚「素材原文如此，但根据 X 应理解为 Y」。

### 1.1 唯一的例外：抓取元数据（2026-09-18 由人类裁定）

#### 为什么需要它

「在 `wiki/` 层更正」这个做法**在行区间这类精细数据上会失效** ——
因为 wiki 页会引用 raw 的行号，一旦 raw 里的行号错了，**所有下游引用都会跟着错**。

2026-09-18 的实例：本库为同一处区间更正了**两次**，而**第一次更正本身也是错的**，直到第三次才对齐。
**「raw 留错 + wiki 更正」在精细数据上不是一条可持续的路。**

#### 但根本解法不是开例外，是字段归位

先看清楚 `capture_note` 里混着两类完全不同的东西：

| 类别 | 例子 | 归谁 |
|---|---|---|
| **抓取事实** | 用了哪个端点、`revision_id`、字符数、下载了哪些资源、有什么已知缺失 | **`raw/`** —— 它描述「这一次抓取」，抓完就固定了 |
| **阅读结论** | 这份素材分几段、每段几行、哪段是 AI 生成的、含几处伪引注 | **`sources/` 页** —— 它是本库的分析产物，**会随阅读深入而变** |

**第二类写在 raw 里必然产生死结**：它随阅读变化，而 raw 不可变。所以：

> **`raw/` 的 frontmatter 只放「抓取事实」。分层表、行区间、缺陷清单一律写在 `sources/` 页。**

下面的例外**只用于清理历史遗留**，不是常态化的可写权限。

#### 准入条件（必须全部满足）

1. **只限 frontmatter，正文一字不可改。** 正文抓取有误 → 走 `-r2` 重抓流程，不走本例外。
2. **只限「本库自产的描述性字段」** —— 白名单：
   `capture_note` / `capture_quality` / `capture_method` / `clipped` / 抓取标识（`revision_id` 等）/ `assets`。
   **黑名单（绝不可改）**：`title` / `author` / `url` / `published` / `kind` ——
   这些是**素材自身的属性**或**指向素材的地址**，改它们等于篡改来源标识，而不只是修正描述。
3. **必须由人类裁定。LLM 不得自行修改。** LLM 只能提出更正请求并附证据（行号 + 该行的实际内容），由人类说「改」。
4. **不得覆盖，只能追加。** 旧值必须留在原地，新值以显式标注追加。
   **「可更正」≠「可覆盖」** —— 允许覆盖就等于允许消灭证据，铁律的精神就没了。
5. **优先「移除」而不是「改正」。** 若某字段里的数据本身就是错的（典型是行区间），
   **把它整段删掉、改为指向 `sources/` 页的指针**，比在原处写一组新数字更安全 ——
   新数字同样可能错，而**指针不会**。这是本例外条款的**首选处置方式**。
6. **改动必须同时做三件事**，缺一不可：
   - `wiki/log.md` 记一条；
   - 对应 `sources/` 页写明「raw 元数据于 X 日更正，原值 Y，依据 Z」；
   - **扫描并更正所有下游引用** —— 否则只是把问题搬了个地方。
7. **一次只改一个字段**，改完立即 `lint`。
8. **尽量用等行数替换。** 2026-09-18 的首次更正暴露了一个 1.1 未预见的问题：
   **改 frontmatter 会改变文件行数，从而使所有行区间失效。**
   那次更正使两份文件分别 **+3** 与 **−5**，**全部下游行号随之平移**。
   **行区间是 raw 的导出量 —— 任何对 raw 的编辑都会使其失效。**
   因此：能用等行数替换就用等行数替换；无法避免时，**必须重新测量全部区间并按第 6 条同步下游**，
   并把新的基准版本（行数）写进相关 `sources/` 页。

#### 行区间必须写明坐标系

2026-09-18 查清的一个具体坑：`raw/` 里两份素材的行区间**都不是文件绝对行号**，
而是**跳过 frontmatter 之后的内容相对行号**（差值恰为 frontmatter 长度：+47 / +39）。
而所有 wiki 页都按**文件绝对行号**引用它们 —— **于是每一条下游引用都偏了一个 frontmatter 的长度。**
这个错误不会自己暴露：偏 39 行仍然指向文件里某个看起来合理的位置。

**规则**：任何行区间断言都必须写明坐标系，默认用**文件绝对行号**（`wc -l` 的坐标系）。
写「行 165–175」之前，回文件 `sed -n '165,175p'` 看一眼。**凭记忆沿用行号是本库已犯过三次的错。**

**怎么识别旧数据用的是哪个坐标系**：若 `capture_method` 里记的「正文 N 行」**小于** `wc -l` 的实际行数，
那 N 就是**内容相对**的行数，差值即 frontmatter 长度 —— 该文件里的全部区间都偏了这么多。
（实例：某素材 `capture_method` 写「正文 15,874 字符 / **441 行**」，而实际文件 487 行，487 − 46 = 441。）

### 三层之外的第 0 问：为什么

三层架构回答的是**「东西放在哪」**。它没有回答一个更靠前的问题：**「为什么要放」**。

没有这个问题，三层会退化成一台自动摘要机 —— 收得越多，熵越大，而没人知道为了什么。
所以 `wiki/projects/` 不是第四层，它是**三层共同的入口**：它给出目标，目标产生误差信号，误差信号决定什么素材值得进 `raw/`、什么页面值得建。

判据来自 [[cybernetic-learning]]：**没有目标，大脑不会标记什么重要。** 这条判据对人和对知识库同样成立。

---

## 2. 目录结构

```
llm-wiki/
├── AGENTS.md              # 本文件：Schema 层
├── README.md              # 给人看的快速开始
├── raw/                   # 原始素材（人类写入，LLM 只读）
│   ├── assets/            #   图片、附件
│   └── YYYY-MM-DD-slug.md #   素材文件
├── wiki/                  # LLM 生成并维护的知识库
│   ├── index.md           #   内容索引（每次 ingest 后必须更新）
│   ├── log.md             #   操作日志（append-only）
│   ├── overview.md        #   总览页：这个知识库在讲什么
│   ├── conventions.md     #   人类的使用偏好（回答风格、格式要求等）
│   ├── projects/          #   项目页：入口层 —— 我在做什么、因此什么重要
│   ├── sources/           #   素材摘要页：一份素材一页
│   ├── entities/          #   实体页：人物、组织、工具、产品
│   ├── concepts/          #   概念页：理论、方法、模式、术语
│   └── analyses/          #   分析页：对比、综述、回答归档
├── tools/
│   └── wiki.py            #   零依赖 CLI 工具链
└── templates/             #   页面模板
```

---

## 3. 页面规范

### 3.1 Frontmatter（每页必需）

```yaml
---
title: 页面标题
type: concept            # source | entity | concept | analysis | meta
slug: llm-wiki-pattern   # 全库唯一，与文件名一致（不含 .md）
tags: [知识管理, LLM, 模式]
created: 2026-09-18
updated: 2026-09-18
sources: [2026-09-18-karpathy-llm-wiki]   # 支撑本页的 raw 素材 slug
related: [rag-vs-wiki, ingest-query-lint] # 相关页面 slug
confidence: high         # high | medium | low —— 证据充分度
status: active           # active | draft | stale | deprecated
---
```

**`slug` 必须与文件名一致**，且全库唯一。工具链靠它解析链接。

### 3.2 页面类型

| type | 目录 | 一页是什么 | 何时创建 |
|---|---|---|---|
| `project` | `projects/` | **我在做的一件事**：目标、当前缺口、消耗的知识、产出 | 人类提出一个要推进的目标 |
| `source` | `sources/` | 一份素材的结构化摘要 | 每次 ingest |
| `entity` | `entities/` | 一个具体的人 / 组织 / 工具 / 产品 | 素材中反复出现的具名对象 |
| `concept` | `concepts/` | 一个抽象的理论 / 方法 / 模式 / 术语 | 素材中反复出现的抽象概念 |
| `analysis` | `analyses/` | 一次对比 / 综述 / 回答归档 | 有价值的问题答案、跨页综合 |
| `meta` | `wiki/` 根 | index / log / overview / conventions | 系统文件 |

**判定规则**：能被指着一张照片说「这是它」的 → entity；只能被描述、不能被拍照的 → concept。
**创建阈值**：一个名字在 ≥2 份素材中出现，或者在一份素材里是核心论点 → 独立成页。只出现一次的次要名词，先在正文里提一句，等它再次出现再建页。

**项目页是唯一的例外 —— 它不由素材派生，而由人类的意图派生。** 所以：
- 它**不需要 `sources`**（lint 已豁免），因为它的来源是「我要做什么」，不是某份素材。
- 它**只能由人类提出**。LLM 不替人类发明项目（见第 8 节分工表）。
- 它是**入口层**：其他页面的价值由「是否服务于某个项目」来检验。

### 3.3 正文结构

每个页面遵循「**先结论，后细节**」：

```markdown
# 标题

> 一句话定义（TL;DR）。读者只看这一行也能知道这页在讲什么。

## 要点
- 3-6 条要点

## 正文
展开论述。用 [[wikilink]] 建立交叉引用。

## 与其他页面的关系
- 区别于 [[x]]：...
- 是 [[y]] 的前置：...

## 待办 / 开放问题
- [ ] 需要补充的资料

## 来源
- [[2026-09-18-karpathy-llm-wiki]]
```

### 3.4 链接语法

- 页内链接：`[[slug]]` 或 `[[slug|显示文本]]`
- **只链接已存在的页面。** 如果 `[[x]]` 指向不存在的页，lint 会报 broken link —— 要么建页，要么去掉链接。
- 每页至少 **1 条出链**（除 `sources/` 外）和 **1 条入链**。孤立页会被 lint 标出。

### 3.5 项目页（入口层）

项目页回答的不是「这是什么」，而是**「我在做什么、因此什么重要」**。它是本库的过滤器生成器 —— 依据来自 [[cybernetic-learning]] 的链条：

```
目标 → 误差信号 → 过滤器 → 相关性 → 留存
```

没有目标，就没有误差信号；没有误差信号，大脑不会标记什么重要。**项目页就是把「目标」和「误差信号」显式写下来的地方。**

#### frontmatter

```yaml
---
title: 项目名
type: project
slug: project-slug
tags: []
created: 2026-09-18
updated: 2026-09-18
goal: 一句话目标，要能被判断「完成没有」   # 项目页特有，必填
stage: planning                          # 项目页特有
started: 2026-09-18
sources: []      # 项目通常留空 —— 它的来源是意图，不是素材
related: []
confidence: high
status: active
---
```

**`stage` 与 `status` 是两个维度，不要混用：**

| 字段 | 回答的问题 | 取值 |
|---|---|---|
| `status` | 这**页**还新鲜吗 | `active` / `draft` / `stale` / `deprecated` |
| `stage` | 这**事**做到哪了 | `planning` / `active` / `paused` / `shipped` / `abandoned` |

#### goal 必须可验收

反例：「学习 Rust」「研究知识管理」。
正例：「用 Rust 写一个能跑的命令行工具并发布到 GitHub」「把本库的历史线补到 1990 年代之前」。

判据：**看到 goal 能回答「做完了没有」**。不能验收的目标产生不了误差信号，等于没写。

#### 正文结构与作用

| 小节 | 作用 | 对应控制论要素 |
|---|---|---|
| 目标（可验收） | 参考信号 | 目标 |
| **当前缺口** | **误差信号** —— 全页最重要的一节 | 误差信号 |
| 知识 | 本项目消耗哪些页面 | 相关性 |
| 产出 | 外向回路 —— 知识流出到哪里 | 留存 / 复用 |
| 决策记录 | 项目里做过的判断（AI 拿不到的部分） | — |
| 不做什么 | 反范围，避免蔓延 | — |

**「当前缺口」是项目页存在的核心理由。** 它必须写成表格：`缺口 / 卡在哪 / 需要什么素材`。这张表直接决定下一步找什么素材。

表格保持三列。需要标优先级时，在「缺口」列内用 `**【阻塞】**` 前缀 —— 它表示这一条直接挡住 `goal` 的验收条件，必须先解；未标的是可选或可延后的。

#### 项目是 ingest 的入口

**这是项目层最重要的作用。** 收录素材前先读 `projects/`，问：

1. 这份素材填的是哪个项目的哪个缺口？
2. 填不上任何缺口 —— 那它**现在还不该收**。

这条规则把「这个内容有意思」换成「这个内容有用」，是 [[cybernetic-learning]] 的过滤器在流程上的落地。**它也是本库对抗「收藏即学习」错觉的主要机制。**

#### 生命周期

- 项目 `shipped` 后**不删除**。它的「决策记录」是后续项目最值钱的参照。
- 项目 `abandoned` 时，在「决策记录」里写清**为什么放弃** —— 这比成功记录更稀缺。
- 项目长期 `paused` 且无产出，考虑降级为 `analyses/` 里的一页，或标 `stage: abandoned`。

#### 项目的终点：缺口表清空即 `shipped`

**项目是有终点的，不是常驻容器。**（2026-09-18 由人类裁定，首个适用者是 [[emotion-awareness]]）

判据很直接：**「当前缺口」表清空 → 该项目 `shipped`。**

**注意是「清空」，不是「没有【阻塞】条目」。** 【阻塞】只是优先级标记，非阻塞的条目也是缺口 ——
一条缺口都没标【阻塞】，只能说明**还没做优先级排序**，不能说明项目做完了。
（`lint` 会检查这两件事：已 shipped 但表未清空 → 报问题；表已清空但项目还开着 → 给提示。）

若某条缺口经过判断决定不做，**移入「不做什么」一节**，不要留在缺口表里 —— 否则表永远清不空，项目永远不能收尾。

清空之后来的新素材，按下面三条分流：

| 情形 | 处理 |
|---|---|
| 新素材填的是**现有缺口** | **不构成新项目** —— 正常 ingest，项目仍是 `active` |
| 缺口表**已清空**（项目已 shipped）后，新素材开的是**新缺口** | **另开一个项目**，原项目保持 `shipped`，**不复活** |
| shipped 后，新素材**推翻了原项目的结论** | **不是新项目，是原项目复活** —— `stage: shipped → active`，并在决策记录里写明是哪条结论被推翻 |

第三行是必要的补丁：否则「另开一个」会漏掉「旧结论被证伪」这种情况，让错误结论留在已 shipped 的项目里无人认领。

> [!warning] 「另开一个」有一个必须防的退化
> **另开项目 ≠ 从零开始。** 如果后继项目不显式继承前任，前面那个项目的全部工作（缺口判定、被否决的方案、伦理风险标记）就会失效，团队会重复劳动、重复踩坑。
>
> 因此规定：**后继项目必须显式继承前任** ——
> - 在「决策记录」里引用前任 slug（`继承 [[前任项目]]：……`）
> - 在缺口表里标明**哪些缺口是继承的、哪些是新出现的**
> - 若前任有被否决的方案或标了伦理风险的做法，在后继项目里**重申一次**
>
> 这条不写进 schema，「另开一个」在实践中就会退化成「重新开始」。**它也是 [[commonplace-book-vs-llm-wiki]] 「燃料而非收藏」在项目层的对应物** —— 一个不被继承的项目，等于一次性的消耗品。

---

## 4. 工作流

### 4.1 Ingest（收录素材）

人类把素材丢进 `raw/`，然后说「收录」。

**第 −1 步（先于一切，且 LLM 不可代劳）：人类先问（HFQ）**（2026-09-18 由人类裁定）

**在 LLM 读素材之前，人类必须亲手写下两样东西**，原样落入 `sources/` 页的 `## 人类先问` 一节：

1. **收录理由** —— 一句话。为什么是这份素材、为什么是现在。
2. **1–3 个自己的问题** —— 你想从它这里知道什么。

**硬约束**：

- **LLM 不得生成、改写、润色、补全这一节，也不得「先起草一版供人类改」。** 人类没给，ingest 就停在这里等。
- 人类的话**原样保留**（可改错别字，不得改措辞）。这一节是**人类在库内留下的唯一亲笔痕迹**，它的价值全在「是谁写的」。
- ingest **结束时**由 LLM 逐条回填，写进 `## 先问回填` —— **即使答案是「素材没有回答」也要写**。后者正是缺口。

**为什么要这条。** 本库此前的实际形态是「人给一个链接，LLM 完成阅读、分层、核查、拆页、建链、回填」——
人在流程里的位置**更接近审批者**。而 [[cognitive-outsourcing]] 记录的问题恰恰是：
**如果「使用顺序」决定效果，那这个库里的人是「先思考」还是「事后审批」？**
HFQ 把这一步显式写进流程 —— **人类先投入，LLM 后编译**，顺序就有了。

**它同时是 [[cybernetic-learning]] 在 ingest 层的落地**：问题是误差信号的来源。
人类没有问题，就没有误差信号；没有误差信号，收录就退化成收藏。

> **过渡期说明**（2026-09-18）：本裁定之前收录的素材**不追补** —— 亲笔内容无法补写，补写即作废。
> `lint` 对缺此节的 source 页给提示，**不计入问题数**。

**第 0 步：读 `wiki/projects/`。** 判断这份素材服务于哪个项目的哪个缺口。
- 能对上 → 在 `sources/` 页里写明「服务于 [[项目slug]] 的哪个缺口」，并在项目页的「知识」一节加回链。
- 对不上任何缺口 → **先向人类指出**：「这份素材填不上任何当前缺口，确定要收吗？」

这不是形式主义。它是 [[cybernetic-learning]] 的过滤器在流程上的落地 —— 见第 3.5 节。
**注意：若本库还没有任何项目页，先提醒人类建一个**（lint 会在知识页 ≥10 时提示）。

0. **抓取质量优先于抓取速度。** 网页正文抓取会丢行内链接、把 `→` 转成 `->`、丢表格分隔符。只要目标提供结构化端点，就用它：
   - GitHub 仓库 → `api.github.com/repos/<owner>/<repo>`（元数据）+ `/readme`（base64 正文）
   - Gist → `gist.githubusercontent.com/<user>/<id>/raw`（正文原文）
   - X / Twitter → `cdn.syndication.twimg.com/tweet-result?id=<id>&token=a`（元数据 + 正文预览）
   - 静态文档站 → 直接取 `.md` 源文件而非渲染后的 HTML

   落盘后**校验一次**：正文里应该有链接的地方是否还有链接。捕获缺陷在收录时发现成本最低。

   **若只能用 AI 中介的抓取工具（如渲染页提取），必须交叉验证。** 用独立元数据源比对（例如 syndication 端点返回的 `preview_text` 与正文开头是否逐字一致），并在素材 frontmatter 的 `capture_quality` / `capture_note` 中如实记录可信度与已知缺失（如图片未能下载）。

   **素材含推广内容时必须在 frontmatter 与摘要页标注利益披露**（作者是产品运营者、带 UTM 链接等），并把相关页面的 `confidence` 降级。

   **素材本身是 AI 生成内容时，按以下约定处理**（2026-09-18 由人类裁定，`kind: ai-conversation` 等）：

   | 步骤 | 要求 |
   |---|---|
   | 收录 | **收**。理由是它作为「对照样本」有价值 —— 它示范了「结构完整度 ≠ 证据强度」 |
   | 定级 | **按内容质量定，不按「是不是 AI 生成的」「AI 生成占多少比例」定**（判据见下） |
   | 引用 | **可以被其他页面引用，但引用时必须标注其 AI 生成性质与 `confidence`** —— 不允许像引用一手材料那样直接引用 |
   | 说明 | frontmatter 写明 `kind` 与收录理由；摘要页写明「低置信度的原因」与「仍然收录的原因」 |

   **为什么不禁止引用**：完全禁止会让这类素材无法参与库内论证，而降级 + 标注保留了它作为反例的功能。**注意区别于「推广内容」** —— 推广内容的问题是可追溯性（能追溯但动机有偏），AI 生成内容的问题是可追溯性为零，两者降级理由不同。

   **降级判据：看内容质量，不看生成来源**（2026-09-18 由人类裁定，**取代**原先「AI 生成即整页 `low`」的做法）：

   > 人类原话：**「基于内容质量判断，很多 AI 生成的内容也是对原材料的扩展与补充。」**

   **因此不设 AI 生成比例阈值** —— 「过半」「三分之二」本身**都不是**降级理由。
   真正的判据是**这段内容在库里承担什么角色**：

   | 内容形态 | 是否降级 | 理由 |
   |---|---|---|
   | **冒充事实断言，且不可追溯**（无任何可核来源） | **降级** | 读者无法自行验证，而它没有声明自己不可核 |
   | **伪造来源 / 含引注使用失真** | **降级或逐条公示** | 走下文对应类别的处置 —— 伪造的是**来源**，必须降级；失真的是**使用**，公示即可 |
   | **对原材料的扩展与补充**（操作化清单、框架化、比喻、可试用的方法） | **不降级** | 它不冒充事实断言，是**显式的加工品**；标注「本段为 AI 生成的加工」即可 |
   | **二手中介但带一手链接**，核查后准确 | **不降级** | 可核性完好；按「核查覆盖率」公示 |
   | **无法核实，但以「方法建议」而非「事实」的姿态出现** | **不降级**，标「（未验证）」 | 与弱引注同型 |

   **判据的判据** —— 问一句：**「这段话如果错了，读者能不能自己发现？」**
   能（有链接、有可试用的操作、有显式的「未验证」标注）→ 保留 `confidence`；
   不能（精确数字 + 模糊归属 + 无链接）→ 降级。

   **这不推翻 [[mental-strength]]**：那份素材降级的理由是**它自称在陈述事实却毫无来源**，
   不是「它是 AI 写的」。**降级的是行为，不是作者身份。**

   **素材的证据问题分五类，处理方式不同，不要混为一谈**（2026-09-18 由人类裁定；前三类由素材摩擦逼出，第四类经裁定入库，第五类由裁定合并而成）：

   | 类别 | 问题性质 | 处理方式 | 先例 |
   |---|---|---|---|
   | 推广内容 | **可追溯，但动机有偏** | 标注利益披露 + 相关页降级 | [[eden]] |
   | 纯 AI 生成 | **可追溯性为零** | **按内容质量定级**（见上）；整页 `low` 只在这段内容**冒充事实断言**时适用 | [[mental-strength]] |
   | **伪引注** | **可追溯性被伪造** —— 精确数字配模糊归属，含年份 / 机构名，制造出「有出处」的假象 | 整页降级 + **显式区分「不可追溯」（判断）与「已核实不存在」（事实）** | [[2026-09-18-art-of-impossible-notes]] |
   | **弱引注** | **可追溯性被放弃** —— 精确数字配「有研究表明」，无年份 / 机构 / 期刊 | **逐条标注「（未验证）」**，不整页降级 | [[2026-09-18-hyperefficient-notes]] |
   | **引注的使用失真**（2026-09-18 裁定，**由「外推式失真」与「错位引用」合并**） | **可追溯性完好，但数字 / 结论的对应关系被搞错** —— 出处真实、论文真实，**只是安错了位置** | **逐条公示「素材说 X ↔ 原文说 Y」**，不整页降级 | [[2026-09-18-how-we-learn-notes]]（外推）、[[2026-09-18-writing-is-thinking]]（错位 4 例） |

   **前三类是「污染」，后两类不是。** 污染指来源本身出了问题，必须整页降级；弱引注（「的确有研究表明……提高 20%」）是**强度不足** —— 它在科普写作里是常规做法，没有骗人，只是**没把话说完整**。**混同会让「引注不规范」看起来像「学术不端」**；反过来，把伪造当成不规范，则会放过更严重的问题。

   **第三类最危险**：前两类看署名就能警觉，伪引注要真去查才发现查不到。

   **「引注的使用失真」的合并理由与处置方式。** 它合并了两个此前的候选：

   - **外推式失真**（范围扩大）—— [[2026-09-18-how-we-learn-notes]] 说「画概念图的效果甚至还不如多读几遍」，
     而 Karpicke & Blunt 2011 *Science* 331:772 只做了「提取练习 vs 概念图」，**摘要里没有「概念图 vs 反复学习」这个比较**。
   - **错位引用**（同类数字错配）—— [[2026-09-18-writing-is-thinking]] 一页给出四例：
     把「心理健康」一列的相关系数当成了「认知」一列；把「84% 是 No Thinking」与
     「thought-skipping 是 almost all」两个层级互换；把产品名与换算都写错。

   **合并理由**：两者同为「数字与结论的对应关系被搞错」，性质、严重度、处置方式一致，
   拆开会让同一类错误看起来像两种问题。

   **它为什么既不能按伪引注处理、也不能按弱引注处理**：那两类的**第一步都是「查有没有出处」**，
   而这里的出处**一查就有、且是对的** —— **查出处这个动作查不出它**。
   它必须**查「这个数字在原文里指向什么」**，即**核对对应关系**，而不是核对存在性。
   它**不是来源污染**（数字和论文都是真的），**按污染处理会误伤**；
   又**不是强度不足**（弱引注没骗人，它把结论安到了别处），**按弱引注处理会漏掉**。

   **处置**：在摘要页的核查表里逐条写清「素材说 X ↔ 原文说 Y」，并给出正确对应关系。
   **不整页降级** —— 降级是给「来源污染」用的，这里是**使用失真的公示问题**。

   **「过期引注」是否也写入本表（单列一类），仍待裁定。** 本库建议单列 ——
   它的识别方式与上述所有类别都不同（要查**该研究现在是否仍被支持**，既不是查出处，也不是查对应关系），
   先例见 [[2026-09-18-extended-mind-notes]]。

   **混层素材必须逐行比对数字。** AI 生成的摘要段会「**补进**」原文没有的具体数字 —— **但不总是**：

   | 素材 | 摘要段的行为 |
   |---|---|
   | [[2026-09-18-art-of-impossible-notes]] | **加法** —— 摘要说「挑战比技能高 **4%**」，成书正文只说「**略超过**技能水平」（有定性、无定量）。「4%」全文 3 处**全部落在摘要段** |
   | [[2026-09-18-hyperefficient-notes]] | **减法** —— 逐行核对后，摘要段的**每个数字都能在讲稿里找到**，且它**漏掉**了讲稿的 5 个百分比 |

   **同一来源、同一形态的两份笔记，摘要段的行为可以完全相反。因此不能按形态一刀切判定，只能逐份逐行核对。**
   核对时必须比对「摘要段」与「正文段」的数字，不能只读摘要 —— **加法式缺陷比整段编造更难察觉：它只加一个数字，读起来更可信。**
   另注意区分两种缺陷：**「书里没有」与「书里有但无引注」不是一回事，不要合并。**

1. **读**：完整读一遍素材，含 frontmatter。如果素材引用图片，先读文本，再单独查看关键图片。
2. **对齐**：向人类汇报 3-5 条关键要点，以及它与现有 wiki 的关系（新增？印证？**矛盾**？）。若素材与已有页面冲突，明确指出，不要悄悄覆盖。
3. **写摘要页**：`wiki/sources/<slug>.md`，按模板。slug 取 `YYYY-MM-DD-短标题`。
4. **拆实体 / 概念页**：按 3.2 的阈值，新建或更新 `entities/`、`concepts/`。
5. **回填**：更新所有被影响的已有页面 —— 补充事实、修订结论、标注矛盾。**一份素材通常触及 10-15 个页面，只写摘要页是失败的做法。**
6. **更新索引**：在 `wiki/index.md` 对应分类下加入新页与一行摘要。
7. **写日志**：`python3 tools/wiki.py log ingest "素材标题"`。
8. **自查**：`python3 tools/wiki.py lint`，确保无 broken link。

### 4.2 Query（查询）

1. 先读 `wiki/index.md` 定位候选页（中等规模下比向量检索更可靠）。
2. 规模变大后用 `python3 tools/wiki.py search "<关键词>"` 做 BM25 检索。
3. 读相关页，**带引用地**综合作答。引用格式：`（见 [[page-slug]]）`。
4. **好答案必须归档。** 如果回答包含对比、综合、新连接 —— 写入 `wiki/analyses/<slug>.md`，并回填索引与日志。
   这样探索也会像素材一样复利。
5. 若发现知识缺口，在回答末尾列出「建议补充的素材 / 建议深挖的问题」。
6. **说「库里没有 X」之前，必须先在库内检索一遍 —— 而且要检索的段落必须说清。**

   「库里没有」是一个**很强的断言**，比「我找到了」更容易说错，因为**没有任何东西会提醒你漏了什么**。

   本库在 2026-09-18 一天之内因此错了**三次**，且三次成因完全相同 ——
   **只检索了混层素材的摘要段与讲稿段，漏掉成书正文第四部分**：

   | 本库曾写 | 事实 |
   |---|---|
   | 「两份素材谁都没有定义心流」 | **错**。Kotler 侧有六特征定义（C 段行 1661–1668） |
   | 伪引注在「摘要段」、共 4 处 | **错**。在**后记段**（行 1944–1972）、共 **5 处** |
   | 「C 段的六特征定义**未点名契克森米哈赖**」 | **误导**。定义段确实没点名，但 **C 段行 1692 紧接着就点名了**，并交代了九 → 六 的修订 |

   操作要求：

   - 说「某素材没有 X」时，必须写明**检索了哪几段**（按分层表的段号 / 行区间）。只说「检索过」不成立。
   - 素材是**混层的**，段与段的性质不同（见 4.1）。在**一段**里没找到，不等于在**素材**里没有。
   - 引用行号前**回原文核对一次**，不要凭记忆沿用 —— 错误的行号会被下游页面继承，且越传越难发现。

   > [!warning] 这不是「谨慎」的问题，是「断言强度」的问题
   > 「**不可追溯**」与「**不存在**」是两种强度的断言，本库已在伪引注上区分过（见 4.1）。
   > 「库里没有」属于后者 —— **说出口前必须有检索记录**。
   > 反过来也一样：若检索后确实没有，**不要为了显得完整而含糊其辞**，直接写「没有」并注明检索范围。

### 4.3 Lint（体检）

人类说「检查一下知识库」时执行。跑 `python3 tools/wiki.py lint` 拿机器可查的部分，再人工/LLM 补下面这些语义问题：

- **矛盾**：两页对同一事实给出不同说法 → 用 `> [!warning] 矛盾` 块标出，并列双方证据。
- **过期**：新素材已推翻旧结论 → 旧页标 `status: stale`，并链接到新页。
- **孤岛**：无入链的页面。
- **该建未建**：被反复提及但没有独立页的概念。
- **数据缺口**：可以靠一次网络搜索补上的空白。
- **建议**：下一批值得找的素材、值得问的问题。
- **项目层**（机器可查的部分工具链已覆盖，语义部分靠人）：
  - 项目页缺 `goal`、`stage` 取值非法、未链接任何知识页 → 工具链直接报错
  - **已 `shipped` 但「当前缺口」表未清空** → 工具链直接报错。这是「假 shipped」：缺口清空才是 shipped 的判据，决定不做的条目应移入「不做什么」
  - **未被任何项目引用的知识页** → 工具链给提示。逐个人工判断：该挂到某个项目下，还是该考虑不再维护？
  - **「当前缺口」表已清空但项目还开着** → 工具链给提示。按第 3.5 节，可以收尾标 `shipped` 了
  - **长期无产出的项目** → 人工核查。项目页「产出」一节为空且 `stage: active` 超过一段时间，要么降级，要么承认它是收藏而非项目。
  - **缺口过期的项目** → 人工核查。「当前缺口」表里的条目是否还成立？已解决的有没有划掉？
  - **没做优先级排序的项目** → 人工核查。`active` 项目若一条【阻塞】都没标，未必是「没有阻塞项」，更可能是**从没做过优先级判断**（尤其在该约定引入之前建的项目）。逐条问：这一条真的不挡 `goal` 的验收吗？

每次 lint 完写一条日志：`python3 tools/wiki.py log lint "本轮发现 N 个问题"`。

---

## 5. index.md 与 log.md

**index.md —— 面向内容。** 全库目录，按分类组织，每条 = 链接 + 一行摘要。每次 ingest 后更新。查询时先读它。

**log.md —— 面向时间。** 只追加，不改写。条目格式固定，便于 grep：

```
## [2026-09-18] ingest | LLM Wiki (Karpathy Gist)
```

保持这个前缀，就可以：`grep "^## \[" wiki/log.md | tail -5`

日志类型：`ingest` / `query` / `lint` / `edit` / `init`。

---

## 6. 工具链

```bash
python3 tools/wiki.py <command>

  init [path]              在当前/指定目录初始化一个新的知识库
  lint                     健康检查：断链、孤岛、缺 frontmatter、重复 slug、项目层
  stats                    统计：各类型页面数、链接数、孤立页、项目阶段分布
  search "<query>"         BM25 全文检索（支持中文二字组）
  index                    从各页 frontmatter 重建 wiki/index.md
  build                    生成单文件浏览站点 site/index.html
  log <type> "<message>"   追加一条日志
  new <type> <slug>        按模板新建页面（type: project / source / entity / concept / analysis）
  graph                    打印链接关系（便于发现枢纽页与孤岛）
```

**零依赖**：只用 Python 标准库，不需要 pip install。

---

## 7. 命名与写作约定

- 文件名：全小写，单词用 `-` 连接，英文 slug（`rag-vs-wiki`）。标题用中文写在 `title` 字段里。
- 素材文件名：`YYYY-MM-DD-slug.md`，日期是**收录日**还是**发布日**取其一并保持一致 —— 本项目用**收录日**。
- **重新抓取同一素材**时，新文件加 `-r2` / `-r3` 后缀（`raw/2026-09-18-slug-r2.md`），**不修改旧文件**。新文件的 frontmatter 用 `supersedes` 指向旧版，用 `capture_note` 说明差异。差异必须同时记入对应的 `sources/` 摘要页。
- 语气：直接、信息密度高。不要「值得注意的是」「综上所述」这类填充词。
- 长度：概念页 60-200 行。超过 300 行说明该拆页。
- 数字与事实必须能追溯到 `sources`。**没有来源的断言要显式标注**：`（未验证）`。
- **`sources` 字段必须指向真正包含该内容的素材。** 如果一页的结论来自某处，而那里没有被收录进 `raw/`，就必须先补录素材，或把 `sources` 留空并显式标注缺口 —— 不允许指向一份并不包含该内容的素材（悬空引用）。
- **二手来源与一手来源冲突时，以一手为准，并记录纠正过程。** 检索到的二手引用常有年份/数字误差（如把 1998 年的演讲记成 1999 年）。拿到原文后若发现不一致，**不要静默采用**，在对应 `sources/` 页与实体页写明「二手来源如此，原文为彼」。这类纠正是知识库的价值之一，不是瑕疵。
- **综合页（`analyses/`）必须逐节点标注证据性质。** 当一页把多份素材串成一条叙述时，**节点之间是「事实」还是「本库的推断」要分开说**。做法：列出每份素材覆盖的区间与其性质（一手/二手/通俗），并单独说明哪些结论属综合判断。默认 `confidence: medium`，除非每个节点都有独立一手来源。
- **单来源的实体页要显式声明。** 若一个实体页的全部内容只来自一份素材（尤其是二手或通俗文献），在页尾用 `> [!note]` 说明，并降 `confidence`。数字类断言（数量、年份、规模）若无一级来源，标注「（未验证）」。

---

## 8. 与人类的分工

| 人类负责 | LLM 负责 |
|---|---|
| **提出并定义项目**（含可验收的 `goal`） | 维护项目页的缺口表、知识链接、决策记录 |
| **每次 ingest 前写下收录理由与自己的问题**（HFQ，4.1 第 −1 步） | 读、分层、核查、拆页、建链、回填 |
| 挑选素材、决定研究方向 | 阅读、摘要、拆页 |
| 提出好问题、判断什么重要 | 交叉引用、回填、维护一致性 |
| 最终判断与取舍 | 标注矛盾、跑 lint、写日志 |
| 修改 `conventions.md` 里的偏好 | 把偏好落实到每一次输出 |

人类不写 wiki。**有两件事 LLM 不能代劳** —— 而且它们是同一条原则的两个位置：

1. **不发明项目。** 项目由**人类的意图**派生，不由素材派生。LLM 可以指出「本库没有项目」，但不能替人类决定做什么。
2. **不代写「人类先问」（HFQ）。** 那一节的价值全在「是谁写的」，LLM 生成即作废。
   人类没给，ingest 就停在原地等。

> **两件事的共同点：目标与问题只能由人提出，LLM 负责把答案找到并整理好。**
> 这正是第 4.1 节 HFQ 那条硬约束的由来 —— 本库此前缺的从来不是「答案的整理」，
> 而是**问题是谁提的**。
